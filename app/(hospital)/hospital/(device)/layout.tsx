// Device surfaces render edge to edge with no app chrome: a lobby kiosk, a
// ceiling-mounted TV board and a consult-room console are each the whole screen.
export default function HospitalDeviceLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
